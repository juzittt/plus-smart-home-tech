spring.application.name=analyzer
server.port=8081

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/analyzer
spring.datasource.username=analyzer
spring.datasource.password=analyzer
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# Kafka
spring.kafka.bootstrap-servers=localhost:9092

# Snapshot Consumer
kafka.consumer.snapshot.group-id=analyzer-snapshot
kafka.topics.snapshots=telemetry.snapshots.v1

# Hub Event Consumer
kafka.consumer.hub.group-id=analyzer-hub
kafka.topics.hubs=telemetry.hubs.v1

# gRPC Client Hub Router
grpc.client.hub-router.address=static://localhost:59090
grpc.client.hub-router.enableKeepAlive=true
grpc.client.hub-router.keepAliveWithoutCalls=true
grpc.client.hub-router.negotiationType=plaintext

# Logging
logging.level.ru.yandex.practicum.telemetry.analyzer=DEBUG
logging.level.org.apache.kafka=WARN
logging.level.org.hibernate.SQL=WARN